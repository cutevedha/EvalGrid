# Usage

## Run demo
```bash
eval-grid run-demo
```

## Run tests
```bash
pytest
```

## Add a new project
1. Create test cases in `datasets/`.
2. Map the task to a capability.
3. Set thresholds.
4. Wire in an adapter for the model or data source.
